============
Installation
============

At the command line::

    $ easy_install mysql-statsd

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv mysql-statsd
    $ pip install mysql-statsd